extends layout

block content
  h1= title
  p Welcome to #{title} 
  each val in messages 
    ul 
      li User's name: #{val.user}
      li User's message: #{val.text}
      li message date: #{val.added} seconds ago